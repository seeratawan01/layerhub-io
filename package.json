{
  "name": "layerhub-io",
  "scripts": {
    "build": "pnpm turbo run build",
    "prepack": "pnpm run build && node ./scripts/prepublish.js"
  },
  "devDependencies": {
    "@changesets/cli": "^2.22.0",
    "turbo": "^1.2.11"
  }
}
